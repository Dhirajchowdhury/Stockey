{
  "name": "stockey",
  "version": "1.0.0",
  "description": "A production-ready, Gen-Z styled stock market analyzer with AI/ML predictions, real-time data, and modern UX.",
  "license": "ISC",
  "author": "Dhiraj",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd stock-analyzer-backend && npm run dev",
    "dev:frontend": "cd stock-analyzer-frontend && npm run dev",
    "install:all": "npm install && cd stock-analyzer-backend && npm install && cd ../stock-analyzer-frontend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
